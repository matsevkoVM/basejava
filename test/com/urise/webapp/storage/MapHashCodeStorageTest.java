package com.urise.webapp.storage;

import static org.junit.jupiter.api.Assertions.*;

class MapHashCodeStorageTest extends AbstractStorageTest{
    public MapHashCodeStorageTest(){
        super(new MapHashCodeStorage());
    }
}